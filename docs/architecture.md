# AARAMBH Architecture

User/PWA -> FastAPI -> services -> MySQL.

AI is used for natural-language profile extraction and ranking assistance.
The rule engine remains deterministic for eligibility.
TF-IDF provides explainable similarity matching.
OCR provides a document-readiness pre-check.
Partner ranking uses distance + availability/capacity.
QR stores a token/reference, not sensitive personal data.
